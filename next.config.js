/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output: 'standalone', // removido: incompatível com "next start" no Railway
};

module.exports = nextConfig;
